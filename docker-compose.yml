# version: '2'

# services:
#   productdetails:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: productdetails
#     ports:
#       - "8080:3001"

version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '80:3001'

